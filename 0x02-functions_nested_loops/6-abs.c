#include "main.h"
#include <stdlib.h>

/**
 * _abs - function computes the absolute value of an integer
 *
 * @n: parameter to transport a number to absoulte
 *
 * Return: Always 0 (success)
*/

int _abs(int n)
{
	int ab = abs(n);

	return (ab);
}
